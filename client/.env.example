# In production, set this to your deployed backend's public URL + /api
# e.g. https://your-backend.onrender.com/api
# Leave empty in local development — Vite proxies /api to the backend automatically.
VITE_API_BASE_URL=
